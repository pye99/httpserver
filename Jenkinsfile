pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
               echo "build"
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment in progress'
            }
        }
    }
}
